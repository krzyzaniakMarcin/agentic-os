# Copy to .env and fill in. .env is git-ignored — never commit real secrets.

# Postgres (event log + memory + kb all live here). Local-only single-user rig;
# pick any password. DATABASE_URL is derived from these in docker-compose.yml.
POSTGRES_DB=agentic_os
POSTGRES_PASSWORD=

# Claude Code CLI auth: an API key, or a long-lived token from `claude setup-token`.
ANTHROPIC_API_KEY=

# Langfuse — self-hosted via this compose (see phase0-plan.md T8). Host points at
# the langfuse-web service T8 adds; keys come from Langfuse on first setup.
# Harmless if unset until T6 wires tracing.
LANGFUSE_HOST=http://langfuse-web:3000
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
